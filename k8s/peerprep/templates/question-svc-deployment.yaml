{{- if .Values.question_svc.enabled -}}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "peerprep.question_svc.name" . }}
  labels: {{- include "peerprep.question_svc.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.question_svc.replicaCount }}
  selector:
    matchLabels:
      {{- include "peerprep.question_svc.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels: {{- include "peerprep.question_svc.selectorLabels" . | nindent 8 }}
    spec:
      containers:
        - name: {{ include "peerprep.question_svc.name" . }}
          image: {{ .Values.question_svc.deployment.image.repository }}:{{ .Values.question_svc.deployment.image.tag | default .Chart.AppVersion }}
          ports:
            - name: {{ .Values.question_svc.deployment.ports.name }}
              containerPort: {{ .Values.question_svc.deployment.ports.containerPort }}
              protocol: TCP
{{- end }}
